extends components/Layout/Layout.pug

block styles
    style.
        .is-active {
            padding: 1rem;
            margin: 1rem;
            border-radius: 4px;
            background-color: #98fff1;
        }

block content
    //- include components/Nav/Nav.pug
    div.card(style="margin: 1rem; padding: 3rem;")
        button.btn.btn-primary(data-toggle="modal" data-target="#security_modal") Open

        button.btn.btn-danger New Button

    .target.is-activeon.is-active.ais-active.is-acticve Just Target

    div.card(style="margin: 1rem; padding: 3rem;")
        p First
        p Second
        p Thrid